openapi: 3.0.1

info:
  title: OpenAPI-CodeGen Test
  description: 'This is a test OpenAPI Spec'
  version: 1.0.0

paths:
  /user:
    put:
      operationId: putUser
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  x-go-name: new-name
                city:
                  type: string
                  x-go-name: new-city
      responses:

components:
  schemas:
    Error:
      properties:
        code:
          type: integer
          format: int32
        message:
          type: string

    User:
      type: object
      additionalProperties: false
      properties:
        name:
          type: string
          x-go-name: NewComponentUserName
